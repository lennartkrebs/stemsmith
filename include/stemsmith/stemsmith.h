/**
 * @file stemsmith.h
 * @brief Umbrella header exposing Stemsmith's public API surface.
 *
 * Including this file gives access to job configuration helpers, the service
 * facade (job submission, handles, callbacks), and the weight fetcher interface.
 */
#pragma once

#include "stemsmith/job_config.h"
#include "stemsmith/service.h"
#include "stemsmith/weight_fetcher.h"
